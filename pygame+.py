import pygame


class addButtonListener():
    events = []
    def __init__(self, object, x, y, width, height) -> None:
        if object == "button":
            self.events.append([])
            



class Button():
    def __init__(self, x, y, width, height, text):
        pygame.rect.draw()

